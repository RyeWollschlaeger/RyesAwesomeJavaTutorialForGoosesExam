package goose.blud.example.animals;

/**
 * Simplest example, just a plain dog that extends {@link AbstractAnimal}, nothing else
 */
public class Dog extends AbstractAnimal {
    public Dog() {
        super(4, "Bark");
    }
}
